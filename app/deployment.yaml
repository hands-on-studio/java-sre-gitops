apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: java-sre
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: asia-south1-docker.pkg.dev/sre-gke-lab/java-sre/order-service:PLACEHOLDER
        ports:
        - containerPort: 8080
