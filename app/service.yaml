apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: java-sre
spec:
  type: LoadBalancer
  selector:
    app: order-service
  ports:
  - port: 80
    name: http
    targetPort: 8080
